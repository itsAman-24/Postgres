<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Profile</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #4e54c8, #8f94fb);
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      flex-direction: column;
    }

    .profile-card {
      background: rgba(255, 255, 255, 0.15);
      padding: 2rem;
      border-radius: 1rem;
      width: 90%;
      max-width: 420px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(15px);
      text-align: center;
      position: relative;
    }

    .profile-avatar {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      border: 3px solid #fff;
      object-fit: cover;
      margin-bottom: 1rem;
    }

    h1 {
      font-size: 1.8rem;
      margin-bottom: 0.5rem;
    }

    p {
      font-size: 1rem;
      opacity: 0.8;
      margin-bottom: 1.5rem;
    }

    .profile-details {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .profile-details li {
      background: rgba(255, 255, 255, 0.2);
      margin: 0.5rem 0;
      padding: 0.7rem;
      border-radius: 0.5rem;
      font-size: 1rem;
      font-weight: bold;
    }

    .profile-details li span {
      font-weight: normal;
      opacity: 0.9;
    }

    .button {
      color: #fff;
      font-weight: bold;
      background: #6a70e0;
      padding: 0.8rem 1.5rem;
      border-radius: 0.5rem;
      text-decoration: none;
      display: inline-block;
      margin-top: 1.5rem;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .button:hover {
      background: #524eb7;
      transform: scale(1.05);
    }

    @media screen and (max-width: 480px) {
      .profile-card {
        padding: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <%- include("partials/navbar") %>

  <div class="profile-card">
    <!-- Profile Picture -->
    <img src="https://ui-avatars.com/api/?name=<%= user.username %>&background=random" alt="User Avatar" class="profile-avatar" />

    <h1><%= user.username %></h1>
    <p><%= user.email %></p>

    <ul class="profile-details">
      <li>ID: <span><%= user.id %></span></li>
      <li>Joined: <span><%= user.created_at || 'N/A' %></span></li>
    </ul>

    <a href="/dashboard" class="button">Back to Dashboard</a>
  </div>
</body>
</html>
